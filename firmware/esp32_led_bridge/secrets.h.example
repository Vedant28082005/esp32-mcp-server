#pragma once

// Copy this file to secrets.h and set your WiFi credentials.
// secrets.h is gitignored — never commit real passwords.

#ifndef WIFI_SSID
#define WIFI_SSID "your-wifi-ssid"
#endif

#ifndef WIFI_PASSWORD
#define WIFI_PASSWORD "your-wifi-password"
#endif
